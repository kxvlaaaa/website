print("hello, welcome to my game of hangman")
print("choose a letter or word")
guess =("input guess a letter between a-z")
hile len(guesses) < maxfalls
    guesses.append(guess)
    guesses.append(guess)
    if guess in word:
    print("you guessed right")
    i = 0
    for char in word:
        if char == guessed
            display_word[i] = guess
        i == 1
guesses[10]
maxfails = 3
if not guess.isnumeric(): # checks if a string is only letters a-z

    my_list = ['k','a','y','l','a']
# Output: p
print(my_list[0])

# Output: o
print(my_list[2])

# Output: e
print(my_list[4])

# Error! Only integer can be used for indexing
# my_list[4.0]

# Nested List
n_list = ["Happy", [2,0,1,1]]

# Nested indexing

# Output: a
print(n_list[0][1])

# Output: a
print(n_list[1][3])
print("you lost oh whale")
